'use client';

import SessionHistoryView from '@/components/rdp/SessionHistoryView';

/** Evidence screen for a desktop session, kept in the leadership shell. */
export default function LeadershipSessionHistoryPage() {
  return <SessionHistoryView />;
}
